-- Hero-блок главной страницы теперь редактируется в админ-центре
-- (Настройки → Главная страница): бейдж, заголовок, описание, текст кнопки
-- для каждого языка + своя картинка. Пустые значения = встроенные тексты
-- по умолчанию, поэтому существующие магазины ничего не заметят.
ALTER TABLE "store_settings" ADD COLUMN IF NOT EXISTS "home_hero" jsonb DEFAULT '{}'::jsonb NOT NULL;
